# Copy to .env and fill in. .env is gitignored; .env.example is checked in
# as documentation of which vars the experiment expects.

# Anthropic API key for the headless classifier / generator / judge calls.
# Generate at console.anthropic.com -> Settings -> API Keys.
ANTHROPIC_API_KEY=
